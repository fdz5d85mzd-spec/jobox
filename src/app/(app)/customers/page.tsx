import Link from"next/link";import{listCustomers}from"@/lib/repositories/customers";import{requireTenant}from"@/lib/tenant";import{CustomerForm}from"./customer-form";export default async function Page(){const t=await requireTenant();const rows=await listCustomers(t.organizationId);return <><div className="pagehead"><div><h1>Customers</h1><div className="muted">{rows.length} CRM records</div></div></div><CustomerForm/><div className="card"><table className="table"><thead><tr><th>Customer</th><th>Email</th><th>Phone</th><th>Status</th></tr></thead><tbody>{rows.map(c=><tr key={c.id}><td><Link href={`/customers/${c.id}`}><b>{c.firstName} {c.lastName}</b></Link></td><td>{c.email||"—"}</td><td>{c.phone||"—"}</td><td>{c.status}</td></tr>)}</tbody></table></div></>}